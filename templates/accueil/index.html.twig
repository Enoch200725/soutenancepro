{% extends 'base.html.twig' %}

{% block body %}
<div class="home-screen">
    <div class="home-seal">
        <svg class="cap-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 3 1 8l11 5 9-4.09V17h2V8L12 3Z" fill="currentColor" stroke="none"></path>
            <path d="M5 10.5V16c0 1.5 3 3 7 3s7-1.5 7-3v-5.5"></path>
        </svg>
    </div>
    <h1>SoutenancePro</h1>
    <p class="home-subtitle">Gestion des Soutenances de Fin d'Études</p>

    <div class="home-options">
        <a href="{{ path('app_administrateur_login') }}" class="home-option">
            <div class="home-option-label">Espace Administrateur</div>
            <div class="home-option-desc">Gestion complete du systeme</div>
        </a>
        <a href="{{ path('app_enseignant_login') }}" class="home-option">
            <div class="home-option-label">Espace Enseignant</div>
            <div class="home-option-desc">Consultation des soutenances</div>
        </a>
    </div>
</div>
{% endblock %}